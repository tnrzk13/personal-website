// Responsive breakpoints - single source of truth.
// CSS media queries use these values directly (e.g. 767px = MD_SCREEN_PX - 1)
export const SM_SCREEN_PX = 640;
export const MD_SCREEN_PX = 768;
export const LG_SCREEN_PX = 1024;
export const COMPACT_BREAKPOINT_PX = SM_SCREEN_PX;
